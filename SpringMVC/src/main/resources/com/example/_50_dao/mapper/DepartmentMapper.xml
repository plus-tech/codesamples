<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example._50_dao.mapper.DepartmentMapper">

    <select id="findAll" resultType="com.example._60_dto.DptDto">
        SELECT department_id, department_name, manager_id 
        FROM departments
        ORDER BY department_id
    </select>
	
    <select id="findById" resultType="com.example._60_dto.DptDto">
        SELECT department_id, department_name, manager_id 
        FROM departments 
        WHERE department_id = #{department_id}
    </select>

    <insert id="insertDpt" parameterType="com.example._60_dto.DptDto">
        INSERT INTO departments (department_id, department_name, manager_id) VALUES (#{department_id}, #{department_name}, #{manager_id})
    </insert>

    <update id="updateDpt" parameterType="com.example._60_dto.DptDto">
        UPDATE departments 
        SET department_id = #{department_id}, department_name = #{department_name}, manager_id = #{manager_id} 
        WHERE department_id = #{department_id}
    </update>

    <delete id="deleteDpt">
        DELETE FROM departments WHERE department_id = #{department_id}
    </delete>

</mapper>